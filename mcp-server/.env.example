# Environment variables for Railway deployment
# Copy this to .env or set in Railway dashboard

# Server port (Railway sets this automatically)
PORT=11235

# Python path (for translation backend)
PYTHON_PATH=python3

# Optional: Enable debug logging
DEBUG=false
